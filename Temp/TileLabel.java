/*package Temp;
import javax.swing.*;
import java.awt.*;


// Probably change to JPanel so we can draw on it
public class TileLabel extends JLabel {
    private int value;
    
    TileLabel() {
        setOpaque(true);
        setBackground(Color.WHITE);

        value = 0;
    }

    public void setValue(int value) {
        this.value = value;
    }

    public int getValue() {
        return value;
    }

    public void displayTile() {
        // Change to display the actual drawn tile
        if (value != 0) {
            setText("" + value);
        }
    }
}*/
 